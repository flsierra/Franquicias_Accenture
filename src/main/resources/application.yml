spring:
  r2dbc:
    url: ${SPRING_R2DBC_URL:r2dbc:mysql://localhost:3306/Franquicias_Accenture}
    username: ${SPRING_R2DBC_USERNAME:root}
    password: ${SPRING_R2DBC_PASSWORD:}

  sql:
    init:
      mode: always

server:
  port: 8080

  springdoc:
    swagger-ui:
      path: /swagger-ui.html