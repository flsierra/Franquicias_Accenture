spring:
  r2dbc:
    url: r2dbc:mysql://localhost:3306/Franquicias_Accenture
    username: root
    password:

  sql:
    init:
      mode: always

server:
  port: 8080

  springdoc:
    swagger-ui:
      path: /swagger-ui.html